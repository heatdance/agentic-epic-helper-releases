{
  "schema_version": 1,
  "_comment": "TEST-PRECON draft+truth: grounded console from runtime_probes. Master: docs/draft-truth-contract.json",
  "requires": ["frozen coverage", "discover.json", "ref.json"],
  "forbidden": ["coverage_pass 2 reinforce dependency"],
  "emit_dod": [
    "coverage.sources.coverage_frozen_at set",
    "console command_patterns use runtime_probes.verified_syntax when present"
  ]
}
