{
  "schema_version": 1,
  "_comment": "scenario_groups[] on -coverage.json after coverage_frozen_at. Playbook: .cursor/pipelines/coverage.md phase 8.6. Verifier: coverage_verify.py --mode draft_truth.",
  "field": "scenario_groups",
  "location": "epics/<KEY>/<KEY>-coverage.json",
  "required_when": "sources.coverage_frozen_at is set",
  "item_schema": {
    "group_id": "sg-001 (unique within epic)",
    "title": "Human-readable scenario group title",
    "check_ids": ["chk-001", "chk-002"],
    "combinatorics": "variant_sequence | single_flow | matrix",
    "source": "principal_thread | section | operator_merge",
    "source_ref": "optional pct-001 or ## section heading"
  },
  "combinatorics_enum": {
    "variant_sequence": "Exercise each check as a sequential variant in one test (e.g. limit then stop then market order)",
    "single_flow": "One narrative flow covering all checks together",
    "matrix": "Cross-product of dimensions when coverage matrix rows imply it"
  },
  "source_enum": {
    "principal_thread": "Derived from ref verification_topology.principal_coverage_threads[]",
    "section": "Derived from Smart Checklist ## section grouping",
    "operator_merge": "Operator merged/split at coverage_review gate"
  },
  "partition_rules": [
    "Every primary check (verification_role primary) appears in exactly one scenario_groups[].check_ids OR in excluded_checks_with_reason on downstream tests",
    "No check_id may appear in more than one group",
    "group_id must be unique",
    "check_ids must reference existing coverage.checks[].id"
  ],
  "materialization": {
    "playbook_phase": "8.6",
    "inputs": [
      "ref.verification_topology.principal_coverage_threads[]",
      "smart_checklist_markdown ## sections",
      "coverage.checks[] primary rows"
    ],
    "human_gate": "coverage_review — operator may merge/split groups before setting coverage_frozen_at"
  },
  "downstream": {
    "test_prep": "One test_bundles[] row per scenario_groups[] entry (1:1)",
    "contract": "docs/test-prep-scenario-intent-contract.json"
  }
}
