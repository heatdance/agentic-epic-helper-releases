# automation/temp

Short-lived agent scratch and local experiments. **Do not commit** large vendor trees (e.g. cloned `aurora-remote-cli`) here; keep the repo small and use Stash/CI for upstream sources.
