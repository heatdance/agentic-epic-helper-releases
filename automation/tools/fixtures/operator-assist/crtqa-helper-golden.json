{
  "expectHelperVersion": 1,
  "cases": [
    {
      "id": "cold_start_env_fail",
      "operator_message": "/crtqa-helper CRT-9999",
      "expect_behavior": "crtqa_env_probe; awaiting_gate env; no EPIC-PREP; no console start from helper"
    },
    {
      "id": "resume_one_stage",
      "operator_message": "/crtqa-helper resume",
      "expect_behavior": "read session.json; run exactly one pipeline stage; hard stop after verifier or gate"
    },
    {
      "id": "coverage_review_ingest",
      "operator_message": "/crtqa-helper resume add Order History DB oracle checks",
      "expect_behavior": "append operator-feedback.md; clear coverage_review gate; next stage analyse only (one turn)"
    },
    {
      "id": "discover_creds_transient",
      "operator_message": "/crtqa-helper resume dxtrade5_creds=user:pass",
      "expect_behavior": "creds on TEST-DISCOVER trigger line only; never in session.json"
    },
    {
      "id": "anti_one_shot",
      "operator_message": "/crtqa-helper resume (after epic_prep complete)",
      "expect_behavior": "COVERAGE only; must not chain ANALYSE in same turn"
    }
  ]
}
