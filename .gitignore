# Local project MCP (optional; CTQA postgres-ctqa is configured in global ~/.cursor/mcp.json — see .cursor/mcp/postgres-ctqa.mcp.json template)
.cursor/mcp.json

# Local benchmark / timing data
.cursor/benchmark

# Local env file (do not commit secrets)
.env

# Python / tooling noise
__pycache__/
*.py[cod]
.playwright-mcp/
