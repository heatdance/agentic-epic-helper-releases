# Optional project-level MCP (never committed). Prefer global ~/.cursor/mcp.json + template .cursor/mcp/postgres-ctqa.mcp.json
.cursor/mcp.json

# Local benchmark / timing data
.cursor/benchmark

# Local env file (do not commit secrets)
.env

# Python / tooling noise
__pycache__/
*.py[cod]
.playwright-mcp/
