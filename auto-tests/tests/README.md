# auto-tests/tests

pytest + Playwright smoke test implementation. **TBD** — no test code in the teach-harness prep pass.

**CLEAN:** `tests/**` is intended for **team** branch when tests are ready to share (private pedagogy under `docs/` and `specs/` remain personal until promoted).
