# auto-tests/tests

pytest + Playwright smoke test implementation. **You build this** in teach phases 1–2 — see [../docs/lessons/README.md](../docs/lessons/README.md).

**CLEAN:** `tests/**` is intended for **team** branch when tests are ready to share (private pedagogy under `docs/` and `specs/` remain personal until promoted).
