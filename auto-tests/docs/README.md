# auto-tests/docs (private)

Operator and agent documentation for smoke automation: environments, credentials pointers (Confluence only — no secrets in repo), pedagogy notes, and delivery runbooks.

**CLEAN:** deleted on `team/team` export; stays on `personal` only.

Implementation guides for pytest and Playwright will land here as the smoke set is defined.
