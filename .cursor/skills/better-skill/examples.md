# better-skill examples (Corner)

## Playbook gap

Draft says “verify coverage” without tool.

**validation_done:** gap — should name `python automation/tools/coverage_verify.py` and required inputs.

**verification_schema:** gap — link `docs/coverage-obligation-contract.json`.

## Good pipeline excerpt

> Phase 8c: run `test_prep_verify.py --mode merge` with `--plan` from temp; delete `{EpicDir}temp/` before emit.

**Good to go** for iterative_subsets + validation_done (applicable rubrics pass).
